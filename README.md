# Python Fundamentals Code Challenge

## 📚 Overview

Welcome to the Python Fundamentals Code Challenge repository! This project showcases solutions to the Phase 3 Code Challenge, focusing on essential Python concepts such as data structures, functions, decorators, sequences, sets, dictionaries, and object-oriented programming.

## 📂 File Structure

- **`solutions.py`**: Contains the Python code for the challenge.
- **`test_solutions.py`**: Contains test cases for verifying the functionality of the code.

- **`README.md`**: 

This file, which provides an overview and setup instructions.

## 🚀 Setup Instructions

Follow these steps to set up and run the code:

1. **Clone the Repository**

   Begin by cloning the repository to your local machine:
   ```bash
   git clone https://github.com/yourusername/your-repository-name.git


Navigate to the Directory

Change into the repository's directory:

bash
Copy code
cd your-repository-name
Run the Code

Make sure Python is installed on your system. You can then execute the script:

bash
Copy code
python solutions.py

🛠 Function Descriptions

add_numbers(num1, num2)

Purpose: Computes the sum of two numbers.
Parameters: Two numbers (integers or floats).
Returns: The sum of the numbers.

is_even(number)

Purpose: Checks if a number is even.
Parameters: A single integer.
Returns: True if even, False otherwise.

reverse_string(text)

Purpose: Reverses the given string.
Parameters: A string.
Returns: The reversed string.

count_vowels(text)

Purpose: Counts vowels (a, e, i, o, u) in a string.
Parameters: A string.
Returns: The count of vowels.

calculate_factorial(n)

Purpose: Calculates the factorial of a non-negative integer.
Parameters: A non-negative integer.
Returns: The factorial of the integer.

apply_decorator(func)

Purpose: Applies a decorator that prints a message before calling the function.
Parameters: A function.
Returns: The decorated function.

sort_by_age(lst)

Purpose: Sorts a list of tuples by age.
Parameters: A list of tuples (name, age).
Returns: The list sorted by age.

merge_dicts(dict1, dict2)

Purpose: Merges two dictionaries, summing values for common keys.
Parameters: Two dictionaries.
Returns: A single merged dictionary.

Car Class

Purpose: Represents a car with make, model, and year attributes.
Attributes:
make: The car's manufacturer.
model: The model name of the car.
year: The year of manufacture.

Method:
display_info(): Prints the car's details.

✨ Notes

Ensure Python is installed on your system to run the script.
The code demonstrates fundamental Python concepts with clarity and best practices.

💡 Contributions

Suggestions for improvements or new features are welcome! Feel free to fork the repository and submit a pull request.

📝 License
This project is licensed under the MIT License - see the LICENSE file for details.

